# Chaos Tendency — <Setting>   (hook: chaos)
# The Chaos Factor step is ALWAYS ±1 per scene (RAW). A setting tunes the LEAN (how readily a
# scene counts as "in control" vs "chaotic") and the FLAVOR (Fate Chart dampening) — not the step.
- start: 5
- control_lean: harsh       # harsh (control is hard-earned; default for hardcore) | even | forgiving
- floor: <none | per-region minimum CF, e.g. "war zones >= 4">
- flavor: standard          # standard (default, full chaos influence) | mid | low | no  (rarely changed)
